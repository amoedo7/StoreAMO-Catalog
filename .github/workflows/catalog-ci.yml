name: catalog-ci

on:
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Paused to stop notification storm
        run: echo "Catalog CI is temporarily manual-only while stale fix branches are cleaned up."
