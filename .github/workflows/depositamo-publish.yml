name: depositamo-publish

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Paused to stop notification storm
        run: echo "DepositAMO publication is temporarily manual-only while the workflow is repaired."
