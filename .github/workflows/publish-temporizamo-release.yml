name: publish-and-verify-temporizamo-release

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Paused to stop notification storm
        run: echo "TemporizAMO publication is temporarily manual-only while distribution integrity is repaired."
